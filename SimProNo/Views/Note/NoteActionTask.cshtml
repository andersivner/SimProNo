@model SimProNo.Models.Note

@if (Model.CompletedDate.HasValue)
{
    @:Completed @Model.CompletedDate
}
else
{
    @Ajax.ActionLink("Done!", "Completed", new { id = Model.Id }, new AjaxOptions() { HttpMethod="Post", UpdateTargetId = "Note" + Model.Id.ToString() })
}
